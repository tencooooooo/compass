name: Compass knowledge graph

on:
  schedule:
    # Weekly graph refresh. JST Monday 09:00 = UTC Monday 00:00.
    - cron: "0 0 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: compass-data-persistence
  cancel-in-progress: false

jobs:
  knowledge-graph:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Restore graph storage
        uses: actions/cache@v4
        with:
          path: storage/knowledge_graph/
          key: compass-knowledge-graph-${{ github.run_id }}
          restore-keys: |
            compass-knowledge-graph-

      - name: Restore persistent Compass data
        run: bash scripts/restore_persistent_data.sh

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -e .

      - name: Run API tests
        run: python -m unittest tests.api.test_api

      - name: Validate restored graph inputs
        run: python -m utils.operational_inputs graph

      - name: Build knowledge graph
        run: python -m lab.knowledge_graph.graph_engine

      - name: Upload graph artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: compass-knowledge-graph
          path: |
            storage/knowledge_graph/
            reports/graph/
          if-no-files-found: ignore

      - name: Persist graph and operational data
        if: always()
        run: bash scripts/persist_persistent_data.sh
